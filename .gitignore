# Raw scan / intermediate pipeline files — never commit these
*.npy
*.nii
*.nii.gz
*.dcm
*.bak

# Python cache
__pycache__/
*.pyc
*.pyo

# Temp segmentation / output dirs
_seg/
_ts_out/
_breast_seg/

# Claude Code internal state
.claude/

# Secrets / environment (never commit real keys)
.env
.env.local
.env.*.local
.vercel

# Node
node_modules/

# Local minified build artifact (Vercel produces this at deploy time; never commit it)
trainer.min.html
