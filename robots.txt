# rtimagematch.com
User-agent: *
Allow: /

# Gated app + checkout have no indexable content (auth required) — keep them out.
Disallow: /trainer
Disallow: /subscribe

# Paid case data (also guarded by edge middleware) — never index the raw assets.
Disallow: /*_data.js$
Disallow: /drr/

Sitemap: https://rtimagematch.com/sitemap.xml
