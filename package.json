{
  "private": true,
  "description": "RT Image Matching Trainer — static site served from root. Build step minifies trainer.html (readable source -> opaque served copy); @vercel/edge powers the interim hotlink middleware.",
  "scripts": {
    "build": "node build-trainer.mjs"
  },
  "dependencies": {
    "@vercel/edge": "^1.1.0"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0"
  }
}
